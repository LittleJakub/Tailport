# build output
bin/
obj/
publish/
*.user

# runtime state (never commit)
*.pid
*.log
*.bak*
config.yaml.bak*

# dev artifacts
*.pdb

# OS
Thumbs.db
.DS_Store

# IDE
.vs/
*.suo

# built exe + config (build.cmd output)
Tailport.exe
Tailport.exe.config

# personal runtime config (holds machine paths + tailnet addresses)
tailport.config
